<div class="container-fluid bg-dark py-4">
    <div class="d-flex justify-content-around align-items-center">
        <h1 class="text-light">Admin Dashboard</h1>
    </div>
    <div class="d-flex justify-content-around mt-1">
        <a href="{{ path('admin') }}" class="btn btn-primary">
            <i class="fas fa-tools fa-2x"></i>
        </a>
        <a href="{{ path('admin_performer') }}" class="btn btn-outline-light">
            <i class="fas fa-user-injured fa-2x"></i>
            Performers
        </a>
        <a href="#" class="btn btn-outline-light">
            <i class="far fa-eye fa-2x"></i>
            Performances
        </a>
        <a href="{{ path('admin_price') }}" class="btn btn-outline-light">
            <i class="fas fa-dollar-sign fa-2x"></i>
            Prix
        </a>
        <a href="{{ path('admin_about') }}" class="btn btn-outline-light">
            <i class="fas fa-home fa-2x"></i>
            About
        </a>
        <a href="{{ path('home') }}" class="btn btn-success" target="_blank">
            <i class="fas fa-igloo fa-2x"></i>
        </a>
    </div>
</div>

{% for label,messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }} mt-3">
            {%  for message in messages %}
                <p> {{ message }}</p>
            {%  endfor %}
        </div>
    </div>
{% endfor %}